export class UserModel {
    token: UserModel;
    roles: any;
    constructor(
        email: string,
        image: string,
        name: string,
        roles: string[],
        token: string,
        username: string
    ) {}

// tslint:disable-next-line: eofline
}